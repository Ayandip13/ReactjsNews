import React from "react";

const Card = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        gap: 40,
        flexWrap: "wrap",
      }}
    ></div>
  );
};

export default Card;
